openapi: 3.0.1
info:
  title: finance data plugin
  description: A plugin that allows you to retrieve financial data for a company based on its ticker symbol.
  version: 'v1'
servers:
  - url: http://localhost:5003
paths:
  /financial_ratios/{ticker}:
    get:
      operationId: getFinancialRatios
      summary: Retrieve financial ratios for a company based on its ticker symbol
      parameters:
        - in: path
          name: ticker
          schema:
            type: string
          required: true
          description: The ticker symbol of the company for which financial ratios are to be fetched.
        - in: query
          name: limit
          schema:
            type: integer
            default: 50
          required: false
          description: The maximum number of financial ratios to retrieve (default is 50).
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                type: array
                items:
                  type: object
                  properties:
                    # Add properties here based on the structure of the JSON response
        "400":
          description: Bad Request
          content:
            text/plain:
              schema:
                type: string

  /stock_news/{ticker}:
    get:
      operationId: getStockNews
      summary: Retrieve recent stock news for a company based on its ticker symbol
      parameters:
        - in: path
          name: ticker
          schema:
            type: string
          required: true
          description: The ticker symbol of the company for which financial ratios are to be fetched.
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                type: array
                items:
                  type: object
                  properties:
                    # Add properties here based on the structure of the JSON response
        "400":
          description: Bad Request
          content:
            text/plain:
              schema:
                type: string


  /earning_call_transcript/{ticker}:
    get:
      operationId: getEarningCallTranscript
      summary: Retrieve recent quater earning call transcript
      parameters:
        - in: path
          name: ticker
          schema:
            type: string
          required: true
          description: The ticker symbol of the company for which financial ratios are to be fetched.
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                type: array
                items:
                  type: object
                  properties:
              
                    # Add properties here based on the structure of the JSON response
        "400":
          description: Bad Request
          content:
            text/plain:
              schema:
                type: string

  /analyst_target_price/{ticker}:
    get:
      operationId: getAnalystTarget
      summary: Retrieve recent analyst target price
      parameters:
        - in: path
          name: ticker
          schema:
            type: string
          required: true
          description: The ticker symbol of the company for which financial ratios are to be fetched.
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                type: array
                items:
                  type: object
                  properties:
              
                    # Add properties here based on the structure of the JSON response
        "400":
          description: Bad Request
          content:
            text/plain:
              schema:
                type: string



components:
  schemas:
    getTodosResponse:
      type: object
      properties:
        todos:
          type: array
          items:
            type: string
          description: The list of todos.
    addTodoRequest:
      type: object
      required:
        - todo
      properties:
        todo:
          type: string
          description: The todo to add to the list.
    deleteTodoRequest:
      type: object
      required:
        - todo_idx
      properties:
        todo_idx:
          type: integer
          description: The index of the todo to delete.
